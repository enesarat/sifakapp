import 'package:flutter/material.dart';

/// Dedicated color palette for the Medication Wizard steps.
class WizardPalette {
  // Primary accent — app blue
  static const Color primary = Color(0xFF13B6EC);
  // Secondary accent — blue-400
  static const Color secondary = Color(0xFF60A5FA);
  // Tertiary accent — sky-400
  static const Color tertiary = Color(0xFF38BDF8);
  // Primary selection color (~27% opacity over primary)
  static const Color primarySelection = Color(0x4413B6EC);
}
